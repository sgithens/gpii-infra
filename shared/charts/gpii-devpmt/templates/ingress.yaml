apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  namespace: {{ .Release.Namespace | quote }}
  name: {{ template "devpmt.name" . }}-ingress
  {{- if .Values.ingress.disableSslRedirect }}
  annotations:
    nginx.ingress.kubernetes.io/ssl-redirect: "false"
  {{- end }}
spec:
  tls:
    - hosts:
      {{ toYaml .Values.dnsNames | indent 6 }}
      secretName: {{ template "devpmt.name" . }}-tls
  rules:
    - host: {{ .Values.dnsNames | first }}
      http:
        paths:
        - path: /
          backend:
            serviceName: {{ template "devpmt.name" . }}
            servicePort: {{ .Values.svcListenPort }}
